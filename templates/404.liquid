{% section 'section-nav' %}
<main style="padding:10rem 2rem;max-width:700px;margin:0 auto;min-height:60vh;text-align:center;">
  <div style="font-family:var(--font-head);font-size:6rem;color:var(--red);line-height:1;">404</div>
  <p style="font-family:var(--font-mono);font-size:0.9rem;color:var(--muted);letter-spacing:0.1em;margin:1rem 0 2rem;">Page not found.</p>
  <a href="{{ routes.root_url }}" style="font-family:var(--font-mono);font-size:0.78rem;letter-spacing:0.2em;text-transform:uppercase;color:var(--chrome-hi);border:1px solid var(--gunmetal);padding:12px 24px;display:inline-block;">&larr; Home</a>
</main>
{% section 'section-footer' %}
